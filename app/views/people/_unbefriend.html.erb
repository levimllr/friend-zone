<%= form_for(current_person.active_relationships.find_by(
    befriended_id: @person.id), html: {method: :delete }, remote: true
    ) do |f| %>
    <%= f.submit "Unbefriend", class: "btn" %>
<% end %>