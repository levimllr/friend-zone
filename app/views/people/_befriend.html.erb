<%= form_for(current_person.active_relationships.build, remote: true) do |f| %>
    <div><%= hidden_field_tag :befriended_id, @person.id %></div>
    <%= f.submit "Befriend", class: "btn btn-primary" %>
<% end %>